{
  "name": "vicoco-seo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "dev": "astro dev --host",
    "check": "astro check",
    "build": "npm run check && astro build && npm run package:static",
    "package:static": "node scripts/package-static.mjs",
    "preview": "astro preview --host"
  },
  "dependencies": {
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
