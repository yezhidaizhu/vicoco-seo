# AGENTS.md

<INSTRUCTIONS>
- This is the SEO content project for `https://vicoco.uk/squoosh-batch-image-compressor/`.
- When adding, renaming, removing, or materially changing an indexable page, review and update its title, meta description, canonical URL, robots meta, Open Graph data, JSON-LD, internal links, sitemap output, `public/robots.txt`, and `public/llms.txt`.
- The production sitemap is generated by `@astrojs/sitemap` from `site` and `base` in `astro.config.mjs`. `public/sitemap-index.xml` and `public/sitemap-0.xml` mirror it for development; keep them synchronized when pages or URLs change. Never hand-edit `dist`.
- Keep `public/robots.txt` pointed at `https://vicoco.uk/squoosh-batch-image-compressor/sitemap-index.xml`.
- Keep `public/llms.txt` aligned with visible features, supported formats, privacy behavior, page URLs, and related Vicoco tools.
- Keep 404 and other non-indexable pages on `noindex` and out of the sitemap.
- The authoritative origin-root robots file is owned by the main `web-squoosh` project. If this project's sitemap URL changes, update that project too.
- Do not run a production build unless the user requests it.
</INSTRUCTIONS>
